# OpenSoundControl parser/encoder

This is a small library for parsing and encoding OSC messages and bundles.

## API

See `src/osc.h` for public functions.

## Running tests

Requires `gtest` library and GCC compiler.

```
make tests
```
